openapi: 3.0.3
info:
  title: Patch Management Platform API
  version: 0.1.0-phase1
  description: >
    Phase 1 contract skeleton. Defines the error envelope and the two endpoints that
    exist today (/health and the RLS diagnostic /diag/assets). Real resource endpoints
    are added by later phases.

    FROZEN AND AUTHORITATIVE - CLAUDE.md section 4.5, resolved by ADR 0021 (2026-08-20),
    closing review finding H5. This file is HAND-AUTHORED AND REVIEWED. It is never generated,
    and no build step rewrites it: a file regenerated from code is an output, not a contract.
    The direction of authority is design-first - the host conforms to THIS FILE, not the reverse.

    Enforced, not merely asserted: OpenApiContractTests compares this file against the real
    endpoint table of a composed host, in BOTH directions. A route the host serves that is absent
    here fails the suite, and so does a path promised here that no route implements. Changing this
    file is a frozen-contract change under NEVER #6 and needs an explicit ask.

    Adding an endpoint therefore starts here: specify it in this file first - which turns the
    suite RED - then implement it until it goes green.

servers:
  - url: http://localhost:5080
    description: Local dev host

paths:
  /health:
    get:
      operationId: getHealth
      summary: Liveness probe.
      tags: [diagnostics]
      responses:
        "200":
          description: Service is up.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /diag/assets:
    get:
      operationId: getDiagAssets
      summary: >
        Diagnostic: list asset hostnames visible to the current tenant. A real
        tenant-scoped read that goes through the restricted DB role + PostgreSQL RLS.
        With no tenant, RLS returns an empty set (fail closed).
      tags: [diagnostics]
      parameters:
        - $ref: "#/components/parameters/TenantHeader"
      responses:
        "200":
          description: The hostnames visible to the resolved tenant (possibly empty).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetsDiagnostic"
        "500":
          $ref: "#/components/responses/Problem"

components:
  parameters:
    TenantHeader:
      name: X-Tenant-Id
      in: header
      required: false
      description: >
        DEV tenant selector. Replaced by an authenticated tenant claim before production.
      schema:
        type: string
        format: uuid

  responses:
    Problem:
      description: An error, as an RFC 7807 problem document.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"

  schemas:
    HealthStatus:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status:
          type: string
          example: ok

    AssetsDiagnostic:
      type: object
      additionalProperties: false
      required: [count, hostnames]
      properties:
        count:
          type: integer
          minimum: 0
        hostnames:
          type: array
          items:
            type: string

    Problem:
      description: RFC 7807 problem details — the standard error envelope for this API.
      type: object
      properties:
        type:
          type: string
          format: uri
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        instance:
          type: string
